import { styled } from "@mui/material/styles";

export const BackToLoginPopup = styled('div')({
    textDecoration: 'underline',
    textAlign: 'left',
    cursor: 'pointer',
    marginBottom: '3rem'
});


export const InsertEmailTitle = styled('div')({
    textAlign: 'right',
    fontSize: '1.25rem',
    fontWeight: 'bold',
    marginBottom: '1rem'
});

export const RightForgotPasswordFailed = styled('span')({
    color: 'red',
    fontSize: '0.75rem',
    marginBottom: '1rem'
});